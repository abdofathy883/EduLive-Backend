@page
@model eLearning_Admin.Pages.CertificateTemplates.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CertificateTemplate[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CertificateTemplate[0].HTMLFilePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CertificateTemplate[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CertificateTemplate[0].UpdatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CertificateTemplate[0].IsDeleted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CertificateTemplate) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HTMLFilePath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeleted)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
